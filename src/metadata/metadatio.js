/**
 * Created by sm on 14/05/16.
 */

import { Map } from 'immutable';

export default class Metadatio {

    constructor() {
        this.configStack = Map({});
    }

    config(key, value) {
        if(value !== undefined) {
            
            
        }
    }

}